import java.util.*;
import java.io.*;
import java.lang.Math;

public class CCC_J2011_J4_Boring {
  
  public static bool checkSafe(String cmd, int dist) {
    
    return true;
  }
  
  public static void main(String args[]) throws IOException {
    
    Scanner scan = new Scanner(System.in); 
       
    while (true) {
         String cmd = scan.next();
         if (a == "q") break;
         int dist = Integer.parseInt(scan.next());
         bool bSafe = checkSafe(cmd, dist);
         System.out.println(bSafe? "safe":"DANGER");
         if (!bool)
           break;
         
         scan.nextLine();
    }
  }
  
}
